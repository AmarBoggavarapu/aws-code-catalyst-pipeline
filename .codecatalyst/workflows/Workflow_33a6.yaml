Name: Workflow_33a6
SchemaVersion: "1.0"

# Optional - Set automatic triggers.
Triggers:
  - Type: Push
    Branches:
      - main

# Required - Define action configurations.
Actions:
  AWSCDKBootstrap_61:
    # Identifies the action. Do not modify this value.
    Identifier: aws/cdk-bootstrap@v2.0.1
    # Required; You can use an environment to access AWS resources.
    Environment:
      Name: s3_bucket_creation_amarbvn_env
    # Specifies the source and/or artifacts to pass to the action as input.
    Inputs:
      # Optional
      Sources:
        - WorkflowSource # This specifies that the action requires this Workflow as a source
    Compute:
      Type: EC2
      Fleet: Linux.x86-64.Large # Defines the action's properties.
    Configuration:
      Region: us-east-1
      CdkCliVersion: 2.1016.1
  AWSCDKDeploy_12:
    # Identifies the action. Do not modify this value.
    Identifier: aws/cdk-deploy@v2.0.2
    # Required; You can use an environment to access AWS resources.
    Environment:
      Name: s3_bucket_creation_amarbvn_env
    # Specifies the source and/or artifacts to pass to the action as input.
    Inputs:
      # Optional
      Sources:
        - WorkflowSource # This specifies that the action requires this Workflow as a source
    Compute:
      Type: EC2
      Fleet: Linux.x86-64.Large # Defines the action's properties.
    Configuration:
      StackName: CdkS3BucketStack
      Region: us-east-1
      CdkCliVersion: 2.1016.1
    DependsOn:
      - AWSCDKBootstrap_61
